<?php

return array(
    /*
     * standardList is to give the application to know if it needs to load a standard list.
     * if checked off it will not trying to preload the full list. you can find the list in the vendor map.
     * you also can use a custom list.
     *
     * posable terms to use:
     * true, (if you want to use the standard list)
     * false, (is you dont want to use a list)
     * custom, (if you want to use a custom list)
     */
    'standardList'=>'true',
    'customList'=>''
);